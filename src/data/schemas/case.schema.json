{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://git-noir.local/schemas/case.schema.json",
  "title": "Git Noir Case",
  "description": "Case definition schema. Case unlocks are progression-based in app logic and are not stored per case.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "title",
    "description",
    "difficulty",
    "total_points",
    "story_intro",
    "steps"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^(beginner|intermediate|advanced)-case-[0-9]{3}$"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "difficulty": {
      "type": "string",
      "enum": ["Beginner", "Intermediate", "Advanced"]
    },
    "total_points": {
      "type": "integer",
      "minimum": 1
    },
    "story_intro": {
      "type": "string",
      "minLength": 1
    },
    "created_at": {
      "type": "string",
      "format": "date"
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "./step.schema.json"
      }
    }
  }
}
